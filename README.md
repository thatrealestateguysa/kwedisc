# Find Your Lead Gen WINWIN (Fixed)
- Relative imports (no @ alias).
- Public logo at /kw-explore-logo.png.
- Netlify build: npm run build, publish dist.

## Local
npm i
npm run dev

## Deploy
- Netlify: build command `npm run build`, publish `dist`.
